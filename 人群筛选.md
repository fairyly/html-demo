# 人群筛选

```
两大块：
  1. 添加逻辑：添加 并且 和 或者 dom;
  2. 每操作一块内容，请求获取数据，追加 dom 结构
  3. 删除后操作

每一个分类带有 name ,id,type = and/or
 
并且: 相当于追加   <最多几个>
或者: 相当于其孩子   <添加几个>

删除 删掉对应的 data-id 对用的 dom


每次选择一个数据字段，都会请求下一个数据字段
(模板会重新获取数据再渲染，dom 事件都写在插件内,传对应得)



```
