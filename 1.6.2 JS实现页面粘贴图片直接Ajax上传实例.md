# JS实现页面粘贴图片直接Ajax上传实例

看了 张鑫旭 写的，也记下看看学习下好了


```
document.addEventListener('paste', function (event) {
    var items = event.clipboardData && event.clipboardData.items;
    var file = null;
    if (items && items.length) {
        // 检索剪切板items
        for (var i = 0; i < items.length; i++) {
            if (items[i].type.indexOf('image') !== -1) {
                file = items[i].getAsFile();
                break;
            }
        }
    }
    // 此时file就是剪切板中的图片文件
});
```

- [demo](https://www.zhangxinxu.com/study/201809/ajax-upload-image-from-clipboard-by-paste.php)


## 参考
- [直接剪切板粘贴上传图片](https://www.zhangxinxu.com/wordpress/2018/09/ajax-upload-image-from-clipboard/)
