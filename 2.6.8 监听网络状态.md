# 2.6.8 监听网络状态

```
window.addEventListener('offline', function() {
  document.getElementById('network').innerHTML = '离线';
});
window.addEventListener('online', function() {
  document.getElementById('network').innerHTML = '在线';
});
```
